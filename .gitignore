# Sensitive personal documents — never commit these
uploads/*redacted*
uploads/Bewerbungsunterlagen*
uploads/ZÜP*
uploads/Aufenthaltstitel*
uploads/Führerschein*

# Design drafts
design-canvas.jsx

# Before/after review screenshots — regenerate, don't commit
.screenshots/

# OS / editor noise
.DS_Store
Thumbs.db
